<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="crw.bishe.team.mapper.auth.AuthUserMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="crw.bishe.team.entity.auth.AuthUser">
                    <id column="USER_ID" property="userId"/>
                    <result column="REVISION" property="revision"/>
                    <result column="CREATED_BY" property="createdBy"/>
                    <result column="CREATED_TIME" property="createdTime"/>
                    <result column="UPDATED_BY" property="updatedBy"/>
                    <result column="UPDATED_TIME" property="updatedTime"/>
                    <result column="USER_NAME" property="userName"/>
                    <result column="PASSWORD" property="password"/>
                    <result column="USER_GROUP_ID" property="userGroupId"/>
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            REVISION, CREATED_BY, CREATED_TIME, UPDATED_BY, UPDATED_TIME, USER_ID, USER_NAME, PASSWORD, USER_GROUP_ID
        </sql>

    <select id="getByNum" resultType="crw.bishe.team.entity.auth.AuthUser">
      select * from AUTH_USER where USER_ID = #{id}
    </select>

    <select id="getAllUsers" resultType="java.lang.Long">
        select count(*) from AUTH_USER
    </select>

</mapper>