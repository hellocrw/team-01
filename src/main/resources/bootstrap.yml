spring:
  application:
    name: auth-system
  cloud:
    config:
      label: master # master分支
      name: auth-system # 配置文件名称
      profile: dev # 读取后缀名称    master 分支下的config-dev.yml配置文件
      uri: http://8.129.110.202:9003  # 配置中心地址
#    nacos:
#      config:
#        server-addr: 127.0.0.1:8848
#        name: example
#        username: nacos
#        password: nacos